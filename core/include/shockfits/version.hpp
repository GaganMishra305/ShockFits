#pragma once

// ShockFits engine version metadata.
// Bump these as bot versions get snapshotted (see PLAN.md Phase 5).

namespace shockfits {

inline constexpr int    kVersionMajor = 0;
inline constexpr int    kVersionMinor = 1;
inline constexpr int    kVersionPatch = 0;
inline constexpr char   kVersionString[] = "0.1.0";
inline constexpr char   kEngineName[]    = "ShockFits";
inline constexpr char   kAuthor[]        = "Gagan";

}  // namespace shockfits
